var c=0
function logItems(loopType){
  for(let n=0;n<loopType.length;n++){
     if(loppType[n]%2==0) continue
   return loppType[n]
  }

  for(var a in loopType){
     if(loopType[a]%2==0) continue
return loopType[a]
  }

for(var m of loopType){
     if(m%2==0) continue
return m
  }

while(c<loopType.length){
       if(loopType[c]%2==0) continue
return loopType[a] 
}
c++

do{
        if(loopType[c]%2==0) continue
        return loopType[a] 
}while(c<loopType.length)
c++
}
}